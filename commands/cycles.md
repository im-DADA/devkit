---
name: cycles
description: PDCA 사이클 목록 조회 — 진행 중인 사이클과 아카이브된 과거 사이클을 한눈에. 특정 사이클의 문서도 열람.
argument-hint: "[사이클명 | 비우면 목록]"
user-invocable: true
allowed-tools:
  - Read
  - Glob
  - Bash
---

# /cycles

사이클 히스토리를 본다. 폴더 규약은 RULES.md "PDCA 사이클" 참조.

## 인자가 없으면 — 목록

1. **진행 중** — `.devkit/pdca-state.json`을 읽어 현재 사이클의 `cycleId`·`stage`·`status`(4필드 전부)를 보여준다. 다음에 할 일은 `PROGRESS.md` 마지막 줄과 `behaviors.json`의 미완료 항목에서 유도한다. 없으면 "진행 중인 사이클 없음".
2. **완료** — `docs/archive/*/*/`를 훑어 아카이브된 사이클을 최신순으로. 각 줄에 날짜·slug·어떤 문서가 있는지(PLAN/DESIGN/GAP/REPORT).
3. `docs/` 최상위에 사이클 폴더가 있는데 상태 파일과 안 맞으면(중단된 사이클) **함께 표시하고 알려준다** — 조용히 넘기지 말 것.

간결한 표로. 사이클이 하나도 없으면 "아직 사이클이 없다"고만 답한다.

## 인자가 있으면 — 해당 사이클 열람

1. 진행 중 폴더(`docs/{인자}*`)와 아카이브(`docs/archive/*/{인자}*`)에서 찾는다. 부분 일치 허용, 여러 개면 후보를 보여주고 되묻는다.
2. 찾으면 그 폴더의 문서 목록과 **REPORT.md 요약**(있으면), 없으면 PLAN.md 목표 문단을 보여준다.
3. 전체 내용이 필요하면 사용자가 파일 경로로 직접 열도록 경로를 명시한다 — 문서를 통째로 채팅에 쏟지 말 것.
