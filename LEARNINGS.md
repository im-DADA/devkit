# LEARNINGS

`/improve`의 결정 기록. **accept뿐 아니라 reject도 남긴다** — 같은 제안이 반복해서 올라오는 것을 막기 위해서다.

규칙은 비대해지면 무시된다. 여기 있는 reject는 "나쁜 아이디어"가 아니라 **"RULES에 넣을 만큼 반복되지 않았거나, 다른 곳에 이미 있는 것"**이다.

---

## 2026-07-25 · evidence 적합성 L3a 사이클 (v0.12.0)

출처 사이클: `docs/archive/2026-07-25/evidence-ref-verification/`

### ✅ accept — 뒤집힌 판단은 원래 기록 자리에 반전 블록을 남긴다

**반영**: `RULES.md` §PDCA 사이클 · 폴더 규약

**근거(실측)**: GAP 2회차가 `.isFile()` 보완을 RED/수정/GREEN까지 갖춰 "닫았다"고 기록했는데, 리뷰 3회차에서 **정책이 반대로 뒤집혔다**(디렉터리 ref를 의도적으로 통과시킴). 뒤집힌 사실을 새 문서(REVIEW)에만 적었더니, GAP.md에는 여전히 "닫았다"가 남아 있었다. **GAP은 REPORT의 직접 입력물이라 그대로 뒀으면 완료 보고서가 "폴더명 위조를 닫았다"는 사실과 다른 성과를 실었을 것이다.** 3회차 리뷰어가 🟡로 잡아 REPORT 직전에 막았다 — **구조가 막은 게 아니라 사람이 막았다.**

**왜 규칙으로 올릴 가치가 있나**: devkit의 PDCA는 `/iterate`·리뷰 재라운드로 **여러 회차를 도는 구조**라 판단 반전이 구조적으로 발생한다. 이번엔 리뷰가 잡았지만 리뷰가 없거나 놓치면 그대로 실린다.

---

### ✅ accept — 검증·판정 로직의 두 번째 처방은 조건을 좁히는 쪽으로

**반영**: `RULES.md` §코드 철학
**주의: 이 항목은 처음에 reject했다가 사용자 지적으로 뒤집었다. 그 오분류 자체가 기록할 가치가 있다(아래).**

**근거(실측)**: 게이트가 정상 파일을 막는 오탐이 났고, 처방이 세 번 나왔다.
1. "URL은 예외로 뺀다" → 고쳤더니 `~/.claude/…`가 막힘
2. "그것도 예외로 뺀다" → 고쳤더니 `.devkit`(이 사이클이 만든 폴더!)이 막힘
3. **방법을 바꿈** — 예외를 얹는 대신 **인정 조건 자체를 좁혔다**(애매하면 통과). 닫혔다.

리뷰어 평가: *"변경이 전부 후보를 빼는 방향이라 오탐으로 뒤집는 경로가 구조적으로 존재하지 않는다. 케이스 테스트가 아니라 **변경의 형태가** 그걸 보장한다."* 1·2차는 구멍을 하나씩 막는 방식이라 새 구멍이 계속 나왔고, 3차는 문턱을 낮춰 애매한 걸 전부 통과시키니 새 구멍이 생길 자리가 없어졌다.

**왜 처음에 reject했고, 왜 틀렸나** — 근거 두 개를 댔는데 둘 다 틀렸다:
- *"HANDOFF 설계원칙 9번에 이미 있다"* → **HANDOFF는 gitignore라 배포되지 않는 devkit 개발용 메모다.** RULES와 독자가 완전히 달라서 중복이 성립하지 않는다.
- *"devkit 내부 얘기지 일반 규칙이 아니다"* → **"devkit 소스를 고치는 일"과 "devkit을 써서 프로젝트를 만드는 일"을 뒤섞었다.** 입력 검증·파서·필터는 어느 프로젝트에서나 짜는 코드고, 이번엔 그게 우연히 devkit의 게이트였을 뿐이다.

→ **교훈: 사례의 표면(이번엔 "게이트")을 보고 특수하다고 분류하지 말 것. 원칙이 무엇에 대한 것인지로 판단해야 한다.** 다음 `/improve`에서 "이건 devkit 전용"이라고 분류할 땐 **"devkit 코드를 손대는 사람만 겪는가?"**를 기준으로 삼을 것 — 그게 아니면 RULES 후보다.

---

### ❌ reject — 새 훅을 도입한 사이클은 그 훅으로 자기를 검증할 수 없다

**이유: devkit 자체 개발 전용이라 RULES에 들어가면 소음이다.** `docs/HANDOFF.md` 설계원칙 10번 + 결함로그 D17에 기록했다.

실측 내용: `hooks.json`에 PostToolUse(Bash)를 추가해도 **그 세션에서는 안 붙는다**(receipts 파일 크기 불변). 이미 등록된 훅의 코드 변경은 즉시 반영된다. 에이전트·커맨드 정의도 같다. 그래서 v0.12.0은 자기가 만든 receipt 층을 자기 사이클에서 실사용 검증하지 못했다(`uncited: 8`이 미확인으로 남음).

RULES는 devkit을 **설치한 모든 프로젝트**에 적용되는 문서인데, 대부분의 프로젝트는 훅을 만들지 않는다.

→ 재제안하지 말 것.

---

### ❌ reject — RED가 불성립일 때 뮤테이션으로 테스트 공허성을 검증한다

**이유: 규칙 없이도 이미 하고 있고, 안 지켜져서 생긴 문제가 없다.**

이번 세션에서 3회 사용했고 매번 유효했다 — E4 보완(이미 구현된 코드라 RED 불성립 → 분기 제거로 테스트가 죽는지 확인), 경계 규칙 4멤버 고정(뮤테이션 4/4 검출), 리뷰어들의 검증(2회차 7/7, 3회차 7/8). 잘 통한 패턴이지만 **마찰이 아니라 이미 정착한 습관**이다.

규칙은 "반복해서 안 지켜지는 것"에 쓴다. 지켜지는 걸 규칙화하면 문서만 길어진다.

→ 이 패턴이 실제로 빠지는 사례가 관측되면 그때 재제안.

---

## 2026-07-26 · receipt cmd 매칭 사이클 (D15·D18·D20)

출처 사이클: `docs/archive/2026-07-26/receipt-cmd-matching/`

### ✅ accept — 불변식이 함수보다 넓으면 테스트도 함수 바깥에서 단언한다

**반영**: `RULES.md` §테스트 / TDD

**근거(실측)**: 같은 실패가 **네 번** 반복됐다. `preview`(quotes/hits만) → `field`(필드만) → `detick`(`formatHuman` 함수만) → R7(프로세스 spawn 경계). 세 번 다 방벽 **자체는 맞았고 범위가 선언한 것보다 좁았다**. 그리고 세 번 다 테스트 이름이 "출력 경로 전체"인데 `formatHuman(v)`를 직접 호출했다.

뮤테이션이 이걸 수치로 보여줬다 — 방벽만 되돌리고 테스트는 그대로 뒀을 때 **"출력 경로 전체"를 주장하던 기존 테스트 3개는 전부 초록, spawn 경계 테스트만 빨강**이었다.

**왜 §코드철학의 "두 번째 처방은 조건을 좁히는 쪽으로"와 중복이 아닌가**: 그 규칙은 ***처방의 형태***를 다룬다. **3회차 `detick`은 그 규칙을 정확히 따랐는데도 샜다** — 함수 방벽을 하나 더 얹는 대신 최종 문자열을 훑는 쪽으로 갔다. 처방은 옳았고 **못 잡은 이유는 테스트가 함수 경계에 서 있어서**였다. 두 규칙이 다른 축이라는 게 이 사이클로 실증됐다.

**LEARNINGS 2026-07-25의 reject("RED 불성립 시 뮤테이션으로 공허성 검증")와도 다르다** — 거기서 기각한 건 이미 정착한 습관이고, 여기선 뮤테이션이 규칙이 아니라 **판별 도구**로 들어간다("되돌렸을 때 빨개지는 테스트가 없으면 그 테스트는 범위를 과장하고 있다").

교착 방지를 위해 대안을 병기했다: **바깥으로 못 올리겠으면 이름에서 '전체'를 빼서 실제 범위에 맞춘다.**

---

### ✅ accept — 규칙을 추가·수정하면 같은 문서의 예시가 그 규칙을 위반하는지 확인한다

**반영**: `RULES.md` §PDCA 사이클 + 정본 스키마 예시 2건 수정

**근거(실측)**: 이 사이클이 `RULES.md:124`에 "cmd는 실제로 돌린 명령을 **그대로** 적어라"를 새로 썼는데, **25줄 위 `:99`의 정본 예시가 그 규칙의 반례**였다:
- `"cmd": "node --test"` — 2토큰 하한과 같아 **사실상 와일드카드**. 복사하면 아무 receipt와도 매칭된다.
- `"output": "✔ … (2.1ms)"` — `✔` 표기가 규범처럼 보여, 손으로 붙인 pytest·go test 사용자가 정직한 evidence로도 `uncited`를 받는다.

**핵심**: AI는 문서를 읽을 때 **산문보다 예시를 복사한다.** 규칙은 해석이 필요하고 예시는 붙여넣으면 되니, 충돌하면 **예시가 이긴다.** 이번엔 리뷰어가 두 회차에 걸쳐 잡았지만(1회차 🟡4 → 2회차 🟡1로 더 깊어짐) 잡은 건 사람이지 구조가 아니었다.

같이 고침: `cmd`를 전체 명령으로, `output`에 "러너가 낸 출력 원문이지 사람이 붙이는 마커가 아니다"를 명시. (테스트 헬퍼 `test/citation.test.mjs:20`의 같은 기본값은 **픽스처라 사용자가 복사하는 예시가 아니고**, 2토큰 하한을 밟는 테스트가 의존할 수 있어 그대로 뒀다.)

---

### ✅ accept — `git checkout <경로>` / `restore` 전에 되돌릴 범위를 본다

**반영**: `RULES.md` §선제 확인 필요

**근거(실측)**: 에이전트가 뮤테이션 원복에 `git checkout hooks/lib/receipt.js`를 썼다가 **앞 단계의 미커밋 `citation.js` 분리 작업까지 되돌렸다.** 폐기한 `SELF_RE`와 구 `checkCitation`이 부활했는데 — **248개 테스트가 전부 green이었다.** 아무도 그 export를 import하지 않아서다. devkit이 잡으려는 데드코드 유형이 자기 레포에서 재현됐다.

`--force`·`reset --hard`·브랜치 삭제는 이미 목록에 있는데 `checkout <경로>`만 빠져 있었다 — 셋 다 되감기인데 하나만 없었다.

**교착 방지**: `checkout <경로>`는 일상적으로 쓰는 명령이라 매번 승인을 걸면 마찰이 크다. 그래서 "확인받아라"가 아니라 **"`git diff <경로>`로 범위를 먼저 보라"**로 낮췄다.

---

### ✅ accept(부분) — 검증 명령에 파이프 금지 → `commands/gap.md`에만. **훅은 다음 사이클**

**반영**: `commands/gap.md` (RULES에는 **의도적으로 안 넣음**)

**근거(실측)**: 이번 세션에서만 **3회** 밟았다(`| tail -6`·`| head -12`·`| tail -8`). stdout이 78자·271자로 잘린 채 봉인돼 그 실행의 증거가 사라졌다(14,920자 → 78자).

**RULES에 안 넣은 이유**: devkit **사용자 전원**이 겪지만 **devkit 워크플로우 안에서만** 겪는다. RULES는 devkit을 설치한 모든 프로젝트에 적용되는 문서이므로 커맨드가 맞는 자리다. (2026-07-25에 세운 기준 "devkit 코드를 손대는 사람만 겪는가?"의 확장 적용 — 이번엔 "코드"가 아니라 "워크플로우" 축에서 갈렸다.)

**⚠ 규칙만으로는 재발을 못 막는다.** `commands/gap.md:31`이 이미 "명령을 **그대로**"라고 못 박고 있었는데도 3번 났다. `/improve` 안티패턴("규칙이 자꾸 안 지켜지면 훅으로 강제할지 검토")에 정확히 해당한다.

→ **다음 사이클 후보**: `bash-receipt.js`가 봉인 시점에 "테스트 러너 호출 + `| head/tail/grep` 또는 `> /dev/null`" 조합을 감지해 **경고**를 남긴다(차단 아님 — 파이프 자체는 정상 명령에서 흔하다). 설계가 필요해 `/improve`에서 즉흥으로 넣지 않았다.
